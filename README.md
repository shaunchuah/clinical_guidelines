# Clinical Guidelines Documentation Site

To get started:

Installation:

```sh
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Development server:

```sh
mkdocs serve
```
